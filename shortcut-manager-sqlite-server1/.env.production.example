# Netlify Environment Configuration
# Copy this to .env.production or set in Netlify Dashboard > Site Settings > Build & deploy > Environment

# Backend Server URL (optional for Light Mode / Offline Mode)
# Leave empty or comment out if you want to run without a backend server.
# Example: https://api.yourdomain.com or https://your-server-ip:5464
VITE_API_URL=
